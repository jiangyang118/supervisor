{
  "name": "mego-morning-check-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "echo 'Use per-app build'",
    "dev": "echo 'Use per-app dev'",
    "test": "echo 'Use per-app test'",
    "demo:mego": "node scripts/mego-demo.js",
    "demo:mego:emit": "node scripts/mego-emit.js"
  }
}
