Placeholders for image fixtures. In this demo, the device-mock CLI uploads small text buffers as files instead of real JPGs to avoid binary bloat in the repo. Replace with actual JPGs if needed for manual testing.

